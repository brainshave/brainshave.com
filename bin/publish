#!/bin/bash -e

if [ -n "$(git diff)" ]; then
  echo "Commit first"
  exit 1
fi

rm -rf dist
node_modules/.bin/broccoli build dist
git checkout master
ls | grep -vE "^(dist|CNAME|node_modules|favicon\.ico)$" | xargs rm -r

mv dist/* .
rmdir dist

git add -A .
git commit -m "Build."
