<?xml version="1.0" encoding="utf-8" ?>
<feed xmlns="http://www.w3.org/2005/Atom">

  {{
    var BASE_URL = 'http://szywon.pl';

    it.id   = BASE_URL + '/' + it.path;
    it.date = it.list[0].date;
  }}

  <title>S/W {{= it.title }}</title>
  <link href="{{= it.id }}" rel="self" />
  <link href="http://szywon.pl" />
  <updated>{{= new Date(it.date).toISOString() }}</updated>
  <id>{{= it.id }}</id>

  <author>
    <name>Szymon Witamborski</name>
    <email>szywon@szywon.pl</email>
  </author>

  {{~ it.list.slice(0, 10) :file }}

  {{ file.id = 'http://szywon.pl/' + file.href; }}

  <entry>
    <title>{{! file.title }}</title>
    <link href="{{= file.href }}" />
    <updated>{{= new Date(file.date).toISOString() }}</updated>
    <id>{{= file.id }}</id>
    <content type="html">{{! file.html }}</content>
  </entry>

  {{~}}

</feed>
